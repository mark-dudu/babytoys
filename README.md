# BabyToys

面向婴幼儿家庭的 Windows 防误触快捷工具。一键进入儿童模式后，程序会覆盖所有显示器、尽量拦截常规键盘鼠标输入，并通过降低画面刺激让电脑迅速变得无聊，引导宝宝自然把注意力从电脑移开。

最新发布版本为 v1.4.0，启动儿童模式后会立即显示低刺激黑色确认界面，更快覆盖全部屏幕，同时保留可靠的取消和家长恢复路径；仍不提供安全锁屏承诺。

## 目标边界

BabyToys 主要服务于宝宝靠近电脑、拍打键盘或鼠标的短暂场景。目标不是把宝宝“锁住”，而是让家长快速把电脑切换成低吸引力状态，减少误操作，并让宝宝更容易自然离开。

产品决策遵循三个原则：

- 家长能够快速启动。
- 电脑能够尽快变得无趣，减少对宝宝的视觉刺激。
- 家长始终拥有可靠、容易理解的恢复路径。

它不是安全锁屏软件，也不承诺拦截 `Ctrl+Alt+Del`、硬件电源键、UAC 安全桌面或所有系统级快捷键。

家长解锁方式：`Ctrl + Alt + U` 长按 3 秒后松开。

触发儿童模式后会立即显示低刺激黑色确认界面；3 秒倒计时期间可点击取消或按 `Esc` 取消。

## 技术栈

- C# / .NET 10 / WPF
- Windows 10/11
- WPF 原生控件
- Win32 P/Invoke 封装系统能力
- 无第三方依赖

## 运行

需要在 Windows 上安装 .NET 10 SDK。

```powershell
dotnet build
dotnet run
```

执行自动化测试：

```powershell
dotnet test --project BabyToys.Tests/BabyToys.Tests.csproj
```

发布单文件版本：

```powershell
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true /p:IncludeNativeLibrariesForSelfExtract=true
```

输出文件位于 `bin/Release/net10.0-windows10.0.17763.0/win-x64/publish/`。

生成带版本号的 ZIP 和 SHA-256 校验文件：

```powershell
./scripts/Publish-Release.ps1
```

输出位于 `artifacts/`。`main` 分支和 Pull Request 会在 Windows GitHub Actions 中执行构建、测试和打包冒烟验证；推送与项目版本匹配的标签后会自动创建 GitHub Release。

## 当前功能

- 主窗口设置图片来源和时间限制，支持小数分钟；时间会限制在 0.5 到 240 分钟之间。
- 默认进入后立即黑屏，也可选择短暂显示系统壁纸或自定义图片后渐暗。
- 启动后立即以低刺激黑色确认界面覆盖所有显示器；主屏保留克制的 3 秒倒计时，可点击取消或按 `Esc` 取消。
- 儿童模式覆盖所有显示器。
- 儿童模式遮罩窗口会隐藏鼠标箭头，退出后由系统自动恢复。
- 图片阶段叠加深色遮罩，10 秒后淡入纯黑屏。
- 使用 low-level keyboard hook 和 mouse hook 尽量拦截常规输入。
- 超时后释放钩子并请求系统睡眠；唤醒后会重新覆盖显示器并恢复输入拦截，继续保持儿童模式直到家长解锁；睡眠请求失败时保持黑屏并允许解锁。
- 配置保存到 `%LocalAppData%/BabyToys/settings.json`。
- 配置使用版本化结构和原子写入；文件损坏时会备份原文件并恢复安全默认值。
- 日志写入 `%LocalAppData%/BabyToys/logs`，限制文件大小并只保留最近 14 个文件；只记录进入、退出、超时和错误，不记录具体按键内容。
- 同一 Windows 会话只允许运行一个 BabyToys 实例，避免托盘、快捷键和设置互相冲突。
- 支持保存、应用和删除多组儿童模式预设。
- 支持 `Ctrl + Alt + B` 全局快捷键启动，仍会经过 3 秒确认倒计时。
- 关闭主窗口后继续在系统托盘运行，可从托盘打开、启动儿童模式或安全退出。
- 可选择登录 Windows 后自动启动到托盘。
- 启动时会核对实际 Windows 启动项与保存设置，并自动清理不一致的残留启动项。
- 若 Windows 调用了缓存的 `--minimized` 启动命令，而设置中未启用开机自启，应用会立即退出。
- 正常儿童模式会维护活动标记；若进程异常终止，下次启动会提示并记录诊断日志。
- 通过 `Ctrl + Alt + Del` 打开并切换到任务管理器时，会立即释放输入拦截和遮罩，保留系统级紧急恢复路径。
- 诊断窗口汇总版本、系统、显示器、配置路径和可唤醒设备，不采集具体按键内容。
- 通过 `powercfg` 只读检查允许唤醒电脑的设备，并提供关闭和恢复唤醒权限的命令指引。

## 已知限制

- 不能阻止 `Ctrl + Alt + Del`、UAC 安全桌面、硬件电源键或所有系统级快捷键。
- 输入拦截依赖 Windows low-level hook，可能受安全软件、权限或系统策略影响。
- 睡眠请求可能被系统电源策略、外接设备或其他应用阻止。
- 当前版本没有安装器、密码解锁或远程解锁。
- 全局快捷键可能被其他程序占用；占用时主窗口会显示提示。

## 手动验证清单

发布或打包前建议在 Windows 10/11 实机验证：

- `dotnet build` 能成功。
- 主窗口可保存图片来源、时间限制和倒计时显示设置。
- 预设可以保存、应用、覆盖和删除。
- `Ctrl + Alt + B` 可以直接显示低刺激确认界面，不会先闪现主窗口。
- 关闭主窗口后托盘图标仍然可用，托盘“退出”可以完整结束程序。
- 开机自启选项可以正确写入和移除当前用户启动项。
- 主窗口“诊断”可以显示系统信息和当前允许唤醒电脑的设备。
- 复制诊断报告不会包含具体按键或输入内容。
- 正常解锁或退出后不会遗留 `active-session.json`；模拟异常终止后，下次启动会显示恢复提示。
- 确认倒计时开始时立即覆盖全部显示器，副屏保持纯黑，主屏只显示低亮度倒计时和取消入口。
- 确认倒计时期间点击取消和按 `Esc` 都能取消，随后主窗口恢复且不遗留遮罩或输入拦截。
- 儿童模式覆盖所有显示器。
- 儿童模式期间所有遮罩屏幕均不显示鼠标箭头，解锁或退出后鼠标箭头恢复。
- 常规键盘和鼠标输入被拦截。
- `Ctrl + Alt + U` 长按 3 秒后松开可以解锁，按键不会泄漏到恢复后的窗口或输入法。
- 长按解锁时所有遮罩屏幕显示剩余秒数和进度条，达到 3 秒后提示“松开即可解锁”。
- `Ctrl + Alt + Del` 打开任务管理器后，儿童模式会自动结束且任务管理器可见、可操作。
- 超时后优先请求系统睡眠。
- 睡眠唤醒后所有显示器继续保持纯黑，常规键鼠仍被拦截，且可以正常解锁和紧急恢复。
- 睡眠前后连接或断开显示器时，遮罩会重新覆盖当前全部显示器。
- 退出或解锁后窗口、计时器和输入钩子被清理，系统恢复正常输入。

## v1.4 发布内容

v1.4 继续缩短从家长触发到电脑变得无趣的时间：

- 点击主窗口、托盘或全局快捷键启动后，立即用黑色确认界面覆盖所有显示器。
- 主屏仅显示低亮度的三秒倒计时和取消入口，副屏保持纯黑。
- 确认阶段不安装输入拦截，家长仍可按 `Esc` 或点击取消。
- 倒计时结束后再安装输入拦截并交接到正式儿童模式，避免重新显示主窗口。
- 取消、退出或遮罩创建失败时完整关闭所有确认窗口，不留下输入钩子或活动会话标记。
- 已通过 Windows 实机验证主窗口、托盘、全局快捷键、多显示器、取消、解锁和紧急恢复路径。

## v1.3 发布内容

v1.3 聚焦“低吸引力体验与唤醒后持续保护”，已按可独立验证的增量完成：

- 默认不再用系统壁纸吸引宝宝，提供立即黑屏或短暂渐暗的低刺激进入方式。（已完成）
- 黑屏状态保持克制，不显示持续动画、明亮提示或不必要的信息。（已完成）
- 超时请求睡眠后，如果电脑再次被键盘、鼠标或其他设备唤醒，继续保持儿童模式，直到家长主动解锁。（已完成）
- 睡眠恢复后重新检查显示器覆盖、输入拦截和紧急恢复监测；恢复失败时安全退出，避免留下无法操作的半锁定状态。（已完成）
- 保持 `Ctrl + Alt + U` 长按解锁和任务管理器紧急恢复路径。（已完成）
- 通过 Windows 实机验证乱拍键鼠、多显示器、睡眠唤醒、设备变化和完整清理。（已完成）

v1.3 暂不加入密码解锁、管理员模式增强、手机远程解锁、安装器或自动更新。这些能力只有在能直接改善快速启动、降低吸引力或可靠恢复时才重新评估。

## 维护与贡献

开发边界、测试命令、提交约定和发布步骤见 [CONTRIBUTING.md](CONTRIBUTING.md)，版本变化见 [CHANGELOG.md](CHANGELOG.md)。

## 许可证

本项目使用 MIT License，详见 [LICENSE](LICENSE)。
